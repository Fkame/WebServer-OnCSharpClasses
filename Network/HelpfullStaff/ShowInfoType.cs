namespace WebServer.Network.HelpfulStaff
{
    /// <summary>
    /// Перечисление, указывающее на то, в каком объёме выводить информацию о том объекте, который использует это перечисление.
    /// </summary>
    public enum ShowInfoType
    {
        ShowFullInfo,
        ShowMinimalInfo,
        ShowNothing
    }
}